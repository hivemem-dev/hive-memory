HIVE_MEMORY_AGENT=claude-code   # agent name shown in memory entries (e.g. claude-code, cursor, codex)
HIVE_MEMORY_PROJECT=/path/to/project   # project id, defaults to cwd if unset
HIVE_MEMORY_DB=./hive-memory.db   # path to the SQLite db file, optional, defaults to ./hive-memory.db
